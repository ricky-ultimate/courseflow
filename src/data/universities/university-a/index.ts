import { cs } from './cs';
import { bio } from './bio';

export const universityA = {
  id: 'university-a',
  name: 'University A',
  departments: [cs, bio],  // Include all departments for the university
};
